can_interface:
  ros__parameters:
    # 绑定 SocketCAN 接口（真实 ZLG 硬件经 zlgcan_bridge 桥接为 can0）
    can_device: "can0"
    can_baud_rate: 500000
    can_loopback: false
    poll_interval_sec: 0.02  # 接收轮询周期，默认 50Hz
    max_steer_deg: 25.0      # Signal2 满量程转向角（deg），与 controller max_steer_angle 一致